version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - echo "No frontend build steps needed"
        build:
          commands:
            - echo "No frontend build steps needed"
        postBuild:
          commands:
            - echo "No frontend post-build steps needed"
      artifacts:
        baseDirectory: .
        files:
          - '**/*'
      cache:
        paths: []
    appRoot: .
    backend:
      phases:
        build:
          commands:
            - npm install
      artifacts:
        baseDirectory: .
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/* 